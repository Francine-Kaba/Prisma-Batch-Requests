# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Callable,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template actions.py.jinja --
import warnings

from . import types, errors
from ._types import BaseModelT


if TYPE_CHECKING:
    from .client import Client


class userActions:
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: 'Client', model: Type['models.user']) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List['models.user']:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.user]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await user.prisma().query_raw(
            'SELECT * FROM user WHERE id = $1',
            1103527590,
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional['models.user']:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.user
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await user.prisma().query_first(
            'SELECT * FROM user WHERE name = $1',
            'dhheabfhf',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.userCreateInput,
        include: Optional[types.userInclude] = None
    ) -> 'models.user':
        """Create a new user record.

        Parameters
        ----------
        data
            user record data
        include
            Specifies which relations should be loaded on the returned user model

        Returns
        -------
        prisma.models.user
            The created user record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a user record from just the required fields
        user = await user.prisma().create(
            data={
                # data to create a user record
                'name': 'ggciceaie',
                'email': 'bbehjachib',
                'password': 'cadfabfehe',
                'created_at': datetime.datetime.utcnow(),
            },
        )
        ```
        """
        resp = await self._client._execute(
            operation='mutation',
            method='createOne',
            model='user',
            arguments={
                'data': data,
                'include': include,
            },
        )
        return self._model.parse_obj(resp['data']['result'])

    async def create_many(
        self,
        data: List[types.userCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple user records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of user record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await user.prisma().create_many(
            data=[
                {
                    # data to create a user record
                    'name': 'dgiiaaijj',
                    'email': 'bfaiacjjfc',
                    'password': 'eigcfgbif',
                    'created_at': datetime.datetime.utcnow(),
                },
                {
                    # data to create a user record
                    'name': 'bagcfbhiig',
                    'email': 'cghideieh',
                    'password': 'biabhbdai',
                    'created_at': datetime.datetime.utcnow(),
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = await self._client._execute(
            operation='mutation',
            method='createMany',
            model='user',
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.userWhereUniqueInput,
        include: Optional[types.userInclude] = None
    ) -> Optional['models.user']:
        """Delete a single user record.

        Parameters
        ----------
        where
            user filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned user model

        Returns
        -------
        prisma.models.user
            The deleted user record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        user = await user.prisma().delete(
            where={
                'id': 836760821,
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                operation='mutation',
                method='deleteOne',
                model='user',
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return self._model.parse_obj(resp['data']['result'])

    async def find_unique(
        self,
        where: types.userWhereUniqueInput,
        include: Optional[types.userInclude] = None
    ) -> Optional['models.user']:
        """Find a unique user record.

        Parameters
        ----------
        where
            user filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned user model

        Returns
        -------
        prisma.models.user
            The found user record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        user = await user.prisma().find_unique(
            where={
                'id': 595337866,
            },
        )
        ```
        """
        resp = await self._client._execute(
            operation='query',
            method='findUnique',
            model='user',
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return self._model.parse_obj(result)

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.userWhereInput] = None,
        cursor: Optional[types.userWhereUniqueInput] = None,
        include: Optional[types.userInclude] = None,
        order: Optional[Union[types.userOrderByInput, List[types.userOrderByInput]]] = None,
    ) -> List['models.user']:
        """Find multiple user records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of user records returned
        skip
            Ignore the first N results
        where
            user filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned user model
        order
            Order the returned user records by any field

        Returns
        -------
        List[prisma.models.user]
            The list of all user records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 user records
        users = await user.prisma().find_many(take=10)

        # find the first 5 user records ordered by the email field
        users = await user.prisma().find_many(
            take=5,
            order={
                'email': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            operation='query',
            method='findMany',
            model='user',
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
            },
        )
        return [self._model.parse_obj(r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.userWhereInput] = None,
        cursor: Optional[types.userWhereUniqueInput] = None,
        include: Optional[types.userInclude] = None,
        order: Optional[Union[types.userOrderByInput, List[types.userOrderByInput]]] = None,
    ) -> Optional['models.user']:
        """Find a single user record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            user filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned user model
        order
            Order the returned user records by any field

        Returns
        -------
        prisma.models.user
            The first user record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second user record ordered by the password field
        user = await user.prisma().find_first(
            skip=1,
            order={
                'password': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            operation='query',
            method='findFirst',
            model='user',
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return self._model.parse_obj(result)

    async def update(
        self,
        data: types.userUpdateInput,
        where: types.userWhereUniqueInput,
        include: Optional[types.userInclude] = None
    ) -> Optional['models.user']:
        """Update a single user record.

        Parameters
        ----------
        data
            user record data specifying what to update
        where
            user filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned user model

        Returns
        -------
        prisma.models.user
            The updated user record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await user.prisma().update(
            where={
                'id': 790425851,
            },
            data={
                # data to update the user record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                operation='mutation',
                method='updateOne',
                model='user',
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return self._model.parse_obj(resp['data']['result'])

    async def upsert(
        self,
        where: types.userWhereUniqueInput,
        data: types.userUpsertInput,
        include: Optional[types.userInclude] = None,
    ) -> 'models.user':
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            user filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned user model

        Returns
        -------
        prisma.models.user
            The created or updated user record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        user = await user.prisma().upsert(
            where={
                'id': 2111915288,
            },
            data={
                'create': {
                    'id': 2111915288,
                    'name': 'bagcfbhiig',
                    'email': 'cghideieh',
                    'password': 'biabhbdai',
                    'created_at': datetime.datetime.utcnow(),
                },
                'update': {
                    'name': 'bagcfbhiig',
                    'email': 'cghideieh',
                    'password': 'biabhbdai',
                    'created_at': datetime.datetime.utcnow(),
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            operation='mutation',
            method='upsertOne',
            model='user',
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return self._model.parse_obj(resp['data']['result'])

    async def update_many(
        self,
        data: types.userUpdateManyMutationInput,
        where: types.userWhereInput,
    ) -> int:
        """Update multiple user records

        Parameters
        ----------
        data
            user data to update the selected user records to
        where
            Filter to select the user records to update

        Returns
        -------
        int
            The total number of user records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all user records
        total = await user.prisma().update_many(
            data={
                'created_at': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            operation='mutation',
            method='updateMany',
            model='user',
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.userWhereInput] = None,
        cursor: Optional[types.userWhereUniqueInput] = None,
    ) -> int:
        """Count the number of user records present in the database

        Parameters
        ----------
        select
            Select the user fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            user filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.userCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await user.prisma().count()

        # results: prisma.types.userCountAggregateOutput
        results = await user.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.userCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.userWhereInput] = None,
        cursor: Optional[types.userWhereUniqueInput] = None,
    ) -> types.userCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.userCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.userWhereInput] = None,
        cursor: Optional[types.userWhereUniqueInput] = None,
    ) -> Union[int, types.userCountAggregateOutput]:
        """Count the number of user records present in the database

        Parameters
        ----------
        select
            Select the user fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            user filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.userCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await user.prisma().count()

        # results: prisma.types.userCountAggregateOutput
        results = await user.prisma().count(
            select={
                '_all': True,
                'name': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            operation='query',
            method='aggregate',
            model='user',
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.userCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.userWhereInput] = None
    ) -> int:
        """Delete multiple user records.

        Parameters
        ----------
        where
            Optional user filter to find the records to be deleted

        Returns
        -------
        int
            The total number of user records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all user records
        total = await user.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            operation='mutation',
            method='deleteMany',
            model='user',
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.userScalarFieldKeys'],
        *,
        where: Optional['types.userWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.userAvgAggregateInput'] = None,
        sum: Optional['types.userSumAggregateInput'] = None,
        min: Optional['types.userMinAggregateInput'] = None,
        max: Optional['types.userMaxAggregateInput'] = None,
        having: Optional['types.userScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.userCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.userScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.userScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.userGroupByOutput']:
        """Group user records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar user fields to group records by
        where
            user filter to select records
        take
            Limit the maximum number of user records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.userGroupByOutput]
            A list of dictionaries representing the user record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group user records by email values
        # and count how many records are in each group
        results = await user.prisma().group_by(
            ['email'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            operation='query',
            method='groupBy',
            model='user',
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class audit_logActions:
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: 'Client', model: Type['models.audit_log']) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List['models.audit_log']:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.audit_log]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await audit_log.prisma().query_raw(
            'SELECT * FROM audit_log WHERE id = $1',
            1149758321,
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional['models.audit_log']:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.audit_log
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await audit_log.prisma().query_first(
            'SELECT * FROM audit_log WHERE timestamp = $1',
            datetime.datetime.utcnow(),
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.audit_logCreateInput,
        include: Optional[types.audit_logInclude] = None
    ) -> 'models.audit_log':
        """Create a new audit_log record.

        Parameters
        ----------
        data
            audit_log record data
        include
            Specifies which relations should be loaded on the returned audit_log model

        Returns
        -------
        prisma.models.audit_log
            The created audit_log record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a audit_log record from just the required fields
        audit_log = await audit_log.prisma().create(
            data={
                # data to create a audit_log record
                'userId': 1644289366,
                'action': 'bdiicjafbj',
            },
        )
        ```
        """
        resp = await self._client._execute(
            operation='mutation',
            method='createOne',
            model='audit_log',
            arguments={
                'data': data,
                'include': include,
            },
        )
        return self._model.parse_obj(resp['data']['result'])

    async def create_many(
        self,
        data: List[types.audit_logCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple audit_log records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of audit_log record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await audit_log.prisma().create_many(
            data=[
                {
                    # data to create a audit_log record
                    'userId': 1647418052,
                    'action': 'bghffegacj',
                },
                {
                    # data to create a audit_log record
                    'userId': 1767274722,
                    'action': 'dcgchcbbf',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = await self._client._execute(
            operation='mutation',
            method='createMany',
            model='audit_log',
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.audit_logWhereUniqueInput,
        include: Optional[types.audit_logInclude] = None
    ) -> Optional['models.audit_log']:
        """Delete a single audit_log record.

        Parameters
        ----------
        where
            audit_log filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned audit_log model

        Returns
        -------
        prisma.models.audit_log
            The deleted audit_log record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        audit_log = await audit_log.prisma().delete(
            where={
                'id': 1343201072,
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                operation='mutation',
                method='deleteOne',
                model='audit_log',
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return self._model.parse_obj(resp['data']['result'])

    async def find_unique(
        self,
        where: types.audit_logWhereUniqueInput,
        include: Optional[types.audit_logInclude] = None
    ) -> Optional['models.audit_log']:
        """Find a unique audit_log record.

        Parameters
        ----------
        where
            audit_log filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned audit_log model

        Returns
        -------
        prisma.models.audit_log
            The found audit_log record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        audit_log = await audit_log.prisma().find_unique(
            where={
                'id': 675780521,
            },
        )
        ```
        """
        resp = await self._client._execute(
            operation='query',
            method='findUnique',
            model='audit_log',
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return self._model.parse_obj(result)

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.audit_logWhereInput] = None,
        cursor: Optional[types.audit_logWhereUniqueInput] = None,
        include: Optional[types.audit_logInclude] = None,
        order: Optional[Union[types.audit_logOrderByInput, List[types.audit_logOrderByInput]]] = None,
    ) -> List['models.audit_log']:
        """Find multiple audit_log records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of audit_log records returned
        skip
            Ignore the first N results
        where
            audit_log filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned audit_log model
        order
            Order the returned audit_log records by any field

        Returns
        -------
        List[prisma.models.audit_log]
            The list of all audit_log records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 audit_log records
        audit_logs = await audit_log.prisma().find_many(take=10)

        # find the first 5 audit_log records ordered by the userId field
        audit_logs = await audit_log.prisma().find_many(
            take=5,
            order={
                'userId': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            operation='query',
            method='findMany',
            model='audit_log',
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
            },
        )
        return [self._model.parse_obj(r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.audit_logWhereInput] = None,
        cursor: Optional[types.audit_logWhereUniqueInput] = None,
        include: Optional[types.audit_logInclude] = None,
        order: Optional[Union[types.audit_logOrderByInput, List[types.audit_logOrderByInput]]] = None,
    ) -> Optional['models.audit_log']:
        """Find a single audit_log record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            audit_log filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned audit_log model
        order
            Order the returned audit_log records by any field

        Returns
        -------
        prisma.models.audit_log
            The first audit_log record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second audit_log record ordered by the action field
        audit_log = await audit_log.prisma().find_first(
            skip=1,
            order={
                'action': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            operation='query',
            method='findFirst',
            model='audit_log',
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return self._model.parse_obj(result)

    async def update(
        self,
        data: types.audit_logUpdateInput,
        where: types.audit_logWhereUniqueInput,
        include: Optional[types.audit_logInclude] = None
    ) -> Optional['models.audit_log']:
        """Update a single audit_log record.

        Parameters
        ----------
        data
            audit_log record data specifying what to update
        where
            audit_log filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned audit_log model

        Returns
        -------
        prisma.models.audit_log
            The updated audit_log record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        audit_log = await audit_log.prisma().update(
            where={
                'id': 744964398,
            },
            data={
                # data to update the audit_log record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                operation='mutation',
                method='updateOne',
                model='audit_log',
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return self._model.parse_obj(resp['data']['result'])

    async def upsert(
        self,
        where: types.audit_logWhereUniqueInput,
        data: types.audit_logUpsertInput,
        include: Optional[types.audit_logInclude] = None,
    ) -> 'models.audit_log':
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            audit_log filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned audit_log model

        Returns
        -------
        prisma.models.audit_log
            The created or updated audit_log record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        audit_log = await audit_log.prisma().upsert(
            where={
                'id': 1969681615,
            },
            data={
                'create': {
                    'id': 1969681615,
                    'userId': 1767274722,
                    'action': 'dcgchcbbf',
                },
                'update': {
                    'userId': 1767274722,
                    'action': 'dcgchcbbf',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            operation='mutation',
            method='upsertOne',
            model='audit_log',
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return self._model.parse_obj(resp['data']['result'])

    async def update_many(
        self,
        data: types.audit_logUpdateManyMutationInput,
        where: types.audit_logWhereInput,
    ) -> int:
        """Update multiple audit_log records

        Parameters
        ----------
        data
            audit_log data to update the selected audit_log records to
        where
            Filter to select the audit_log records to update

        Returns
        -------
        int
            The total number of audit_log records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all audit_log records
        total = await audit_log.prisma().update_many(
            data={
                'details': 'bbbgbhfjge'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            operation='mutation',
            method='updateMany',
            model='audit_log',
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.audit_logWhereInput] = None,
        cursor: Optional[types.audit_logWhereUniqueInput] = None,
    ) -> int:
        """Count the number of audit_log records present in the database

        Parameters
        ----------
        select
            Select the audit_log fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            audit_log filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.audit_logCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await audit_log.prisma().count()

        # results: prisma.types.audit_logCountAggregateOutput
        results = await audit_log.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.audit_logCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.audit_logWhereInput] = None,
        cursor: Optional[types.audit_logWhereUniqueInput] = None,
    ) -> types.audit_logCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.audit_logCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.audit_logWhereInput] = None,
        cursor: Optional[types.audit_logWhereUniqueInput] = None,
    ) -> Union[int, types.audit_logCountAggregateOutput]:
        """Count the number of audit_log records present in the database

        Parameters
        ----------
        select
            Select the audit_log fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            audit_log filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.audit_logCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await audit_log.prisma().count()

        # results: prisma.types.audit_logCountAggregateOutput
        results = await audit_log.prisma().count(
            select={
                '_all': True,
                'timestamp': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            operation='query',
            method='aggregate',
            model='audit_log',
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.audit_logCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.audit_logWhereInput] = None
    ) -> int:
        """Delete multiple audit_log records.

        Parameters
        ----------
        where
            Optional audit_log filter to find the records to be deleted

        Returns
        -------
        int
            The total number of audit_log records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all audit_log records
        total = await audit_log.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            operation='mutation',
            method='deleteMany',
            model='audit_log',
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.audit_logScalarFieldKeys'],
        *,
        where: Optional['types.audit_logWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.audit_logAvgAggregateInput'] = None,
        sum: Optional['types.audit_logSumAggregateInput'] = None,
        min: Optional['types.audit_logMinAggregateInput'] = None,
        max: Optional['types.audit_logMaxAggregateInput'] = None,
        having: Optional['types.audit_logScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.audit_logCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.audit_logScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.audit_logScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.audit_logGroupByOutput']:
        """Group audit_log records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar audit_log fields to group records by
        where
            audit_log filter to select records
        take
            Limit the maximum number of audit_log records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.audit_logGroupByOutput]
            A list of dictionaries representing the audit_log record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group audit_log records by userId values
        # and count how many records are in each group
        results = await audit_log.prisma().group_by(
            ['userId'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            operation='query',
            method='groupBy',
            model='audit_log',
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]



def _select_fields(root: str, select: Mapping[str, Any]) -> str:
    """Helper to build a GraphQL selection string

    This is a work around until field selection is added to the query builder.
    """

    return root + ' {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))


from . import models
